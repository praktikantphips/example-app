# Phipsis example app

> tiny example to learn basics

## Build Setup

``` bash
# install dependencies
$ npm install

# run tests (not complete)
npm run test

# serve at localhost:8080
$ quasar dev mat
